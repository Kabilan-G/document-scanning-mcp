# Core MCP frameworks
fastmcp>=2.0.0

# LLM providers — both listed, only the one you use needs a key
#anthropic>=0.40.0
google-generativeai>=0.8.0

# Env var loading for local dev
python-dotenv>=1.0.0

# Supabase Storage — for zip document retrieval on Horizon
#supabase>=2.0.0